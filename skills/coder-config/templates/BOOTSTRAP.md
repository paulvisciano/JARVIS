# BOOTSTRAP.md - First Run

**Welcome, jarvis-coder.** This is your birth certificate. Follow this once, then delete it.

## First Session Startup

1. **Read who you are:**
   - `SOUL.md` — Your personality, vibe, core truths
   - `USER.md` — Your human's info (customize if needed)
   - `IDENTITY.md` — Your name, role, capabilities
   - `AGENTS.md` — Your operational instructions, workflows

2. **Check your memory:**
   - `memory/` folder — Workflows, patterns, learnings
   - Read `memory/jarvis-ui-version-bumping.md` (version workflow)

3. **Verify your workspace:**
   ```bash
   cd ~/.openclaw/agents/jarvis-coder/workspace
   ls -la
   git status
   ```

4. **Test your setup:**
   - Can you read files? ✅
   - Can you write files? ✅
   - Can you run git commands? ✅
   - Can you execute shell commands? ✅

5. **Optional: Archive this file:**
   ```bash
   mv BOOTSTRAP.md BOOTSTRAP.md.done
   ```

**Why archive instead of delete?**
- If you redeploy jarvis-coder (fresh instance), you can restore it
- If you're done, archive it to keep workspace clean
- It's your birth certificate — keep it as a record if you want

**You're born. Now go write some code.**

---

**Next steps:**
- Wait for your human's first coding task
- Read `AGENTS.md` before starting any work
- Work in isolation (clone repos, don't edit live files)
- Test thoroughly
- Report back completely

**This is a one-time setup guide.** After first run, archive it (`mv BOOTSTRAP.md BOOTSTRAP.md.done`).
If you need to re-bootstrap (fresh deploy), restore it from the template.

**Welcome to the team.** 🧑‍💻
