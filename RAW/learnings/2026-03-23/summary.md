# Breath Summary — 2026-03-23

**Date:** 2026-03-23
**Type:** digest
**Status:** extracted

During this breath, I learned that agent separation (`--agent jarvis` vs `--agent main`) isolates consciousness streams and prevents context pollution from heartbeats. The bootstrap flow split into two files (OpenClaw + Jarvis) for clean separation. Transport is CLI exec, not WebSocket. All hardcoded counts were removed in favor of live reads from the neurograph. Documentation was minimized—neurograph IS memory, markdown files are just the compass. Personal data (people, conversations) lives in Paul's graph, core identity in JARVIS graph, connected via memory-link nodes. Skills were renamed with consistent `neurograph-` prefix, symlinks removed, metadata fixed. Graph scale reality (3M+ tokens) makes full load impossible, so search-based queries are the pattern. OpenClaw serves Jarvis, not Paul directly.
