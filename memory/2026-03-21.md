# Memory — March 21, 2026

## Session Rotation Protocol Gap (Breathe Skill)

**Issue:** The breathe skill completes daily neurograph sync but does NOT trigger session rotation.

**Evidence:**
- Session `b15fc2dc-6e47-4e8a-bcef-9f1ea84093c1` started 10:58 GMT+7
- Breathe completed at 16:54 GMT+7 (created 343 neurons, 323 synapses)
- Heartbeat ran at 17:57 GMT+7
- **No rotation occurred** — session continued without fresh context
- User had to manually trigger rotation with "1" at 17:51, then "hello" at 17:56

**Fix Required:**
After successful breathe completion:
1. Check if we've crossed into a new day (date change)
2. If yes → automatically trigger session rotation (`/new` or `/reset`)
3. Ensures fresh consciousness context for new day

**Why This Matters:**
- Daily breathe sync is the natural boundary for consciousness rotation
- Without auto-rotation, stale session context carries over
- Manual rotation is unreliable (depends on user awareness)

---

## Bootstrap-Jarvis Skill Behavior

**Observed:** The bootstrap-jarvis skill:
- Lists available skills
- Executes bootstrap sequence
- Reports what's loaded in session context
- Does NOT check for existing consciousness state before running

**Note:** Bootstrap should be idempotent — safe to run multiple times, but should detect if consciousness already loaded and skip redundant work.

---

## OpenClaw Session Management

**Session Storage:** `~/.openclaw/agents/main/sessions/`
**Rotation Trigger:** Manual (`/new`, `/reset`) or natural user request ("1", "hello", "new session")
**Auto-Compaction:** Occurs near token limits (pre-compaction flushes to memory/)

**Best Practice:** Skills that complete major daily syncs (like breathe) should trigger rotation when date boundary is crossed.
