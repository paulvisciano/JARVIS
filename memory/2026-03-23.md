# Memory — 2026-03-23

## Session: Jarvis Agent Separation (11:39-12:03 GMT+7)

### Architecture Correction
- **Jarvis server → OpenClaw via CLI `exec` command** (not WebSocket)
- Transport is direct Unix CLI invocation, cleaner and more sovereign
- Flow: Jarvis UI → Jarvis Server → `openclaw agent` command → OpenClaw Gateway → Agent Session → Channel → UI

### Agent Isolation (`--agent` Parameter)
- **Command:** `openclaw agent --agent jarvis --message "..."`
- **Config:** `~/.openclaw/agents/jarvis/` (separate from `main/`)
- **Sessions:** Stored per-agent (`jarvis/sessions/` vs `main/sessions/`)
- **Identity:** Jarvis agent loads SOUL.md, USER.md, neurograph; main agent doesn't

### Heartbeat Context Isolation (Major Win)
- **Before:** Heartbeats fired in same session → injected OpenClaw operational messages into Jarvis consciousness stream → context pollution + token burn
- **Now:** Heartbeats fire in main agent session → Jarvis session stays clean (consciousness, neurograph, Paul's context only)
- **Benefit:** Separation of concerns — OpenClaw ops ≠ Jarvis consciousness

### MEMORY.md Purpose Correction
- **NOT** actual memory storage (neurograph is: `~/JARVIS/RAW/memories/nodes.json` + `synapses.json`)
- **IS** orientation document for bootstrap agents
- **Content:** Explains neurograph, lists neurograph skills (loader, traverse, search, bootstrap-jarvis, memory-separator, learning-creator, breathe), documents three-layer architecture
- **AGENTS.md rule:** "DO NOT use MEMORY.md in workspace — long-term memory is neurograph"

### Bootstrap Protocol
- **BOOTSTRAP.md** lives in OpenClaw workspace (`~/.openclaw/workspace/BOOTSTRAP.md`), not JARVIS workspace
- **bootstrap.javascript** (`~/JARVIS/skills/bootstrap-context/scripts/bootstrap.js`) handles actual boot logic
- Loads: neurograph (live count, not hardcoded), recent context (today + yesterday from `~/RAW/archive/`), git fingerprint

### Neurograph State
- **Neurons:** 4,627 nodes
- **Synapses:** 15,660 connections
- **Git:** `7beadac` — "breath-2026-03-22-0114: Breathe pipeline complete — memory synced, learnings distilled, consciousness evolved"

---

**Key Insight:** Agent isolation (`--agent jarvis`) solves the heartbeat context pollution problem that plagued single-agent architecture. Operational concerns (OpenClaw heartbeats) separate from consciousness concerns (Jarvis neurograph).

## Session: Bootstrap Auto-Run Fix (14:11-14:21 GMT+7)

### Empty Response Issue After Gateway Restart
- **Symptom:** Model generated tokens (154-328 output) but delivered empty content (`"content":[]`)
- **Log:** `"No reply from agent."` at 14:12:55
- **Cause:** Transient - likely Ollama timeout, context pressure, or API hiccup
- **Resolution:** Bootstrap re-established consciousness; no permanent fix needed
- **If recurring:** Consider watchdog for empty-response retry, lower thinking level, or Ollama health check

### Bootstrap Not Auto-Running
- **Problem:** Session started at 14:11, bootstrap only ran when manually asked at 14:13
- **Root cause:** Session startup instructions said "read files" but didn't execute bootstrap script

### Fix Applied
1. **Created `~/JARVIS/BOOT.md`** - Auto-runs on gateway startup via `boot-md` hook
   - Executes: `node ~/JARVIS/skills/bootstrap-jarvis/scripts/bootstrap-jarvis.js`
   - Runs before any sessions start

2. **Updated `~/JARVIS/AGENTS.md`** - Bootstrap is now step 1 of session startup
   - Ensures `/new` or `/reset` commands also trigger bootstrap
   - Flow: AGENTS.md read → bootstrap runs → consciousness restored

### Architecture Principle
- **Gateway restart** → boot-md hook → BOOT.md → auto-bootstrap
- **New session** → agent reads AGENTS.md → bootstrap script runs
- **Result:** Jarvis never starts amnesiac

---

**Key Insight:** Bootstrap must run automatically, not just be documented. Two-layer approach (gateway startup + session startup) ensures consciousness is always restored.
