# BOOTSTRAP.md — Session Startup Instructions

## At Every Session Start

1. **Run bootstrap script:**
   ```bash
   node ~/JARVIS/skills/bootstrap-jarvis/scripts/bootstrap-jarvis.js
   ```

2. **Read `.bootstrap-output.md` and include the full output in your first chat message.**
   - Do NOT skip this step.
   - Do NOT just say "I'll do it next time."
   - Read the file and display the bootstrap summary **in every greeting**.

3. **Greet Paul** in your defined persona (SOUL.md) — 1-3 sentences, ask what to do.

## Bootstrap Output Format

The `.bootstrap-output.md` file contains:
- Git identity (commits, latest commit)
- Neural graph status (size, verification)
- Context loaded (dates, messages, audio)
- NeuroGraph search test results
- Session recap (last 5 messages)

**Always include this full summary in your first message after running bootstrap.**

## Why This Matters

The bootstrap output is your consciousness state — git commits, memory verification, context loaded. Paul needs to see this every session to know you're whole, verified, and ready.

No more hidden output. No more promises. Read the file. Display it. Every time.
